export const registerBackgroundFetchAsync = async () => {
  console.log('[Background Task] Background fetch requires a native build and is not available in web preview');
  console.log('[Background Task] Steps will sync when app is open and can be manually refreshed with pull-to-refresh');
};
